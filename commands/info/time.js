const discord = require('discord.js')
const moment = require('moment');
module.exports={
    name:"time",
    description:"sends Melbourne Time!",
    category: "info",
    usage: "<prefix>time",
    run: async (bot, message, args) => {
        
    }
}